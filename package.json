{
  "name": "magiq-tests",
  "version": "0.0.3",
  "description": "Library for testing visuals",
  "main": "index.js",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/magiq-tests/magiq-tests"
  },
  "scripts": {
    "build": "rimraf dist && json2module package.json > src/package.js && rollup -c",
    "dev": "rimraf dist && json2module package.json > src/package.js && rollup -c -w",
    "prepublish": "rimraf dist && json2module package.json > src/package.js && rollup -c"
  },
  "devDependencies": {
    "html-minifier": "3",
    "jquery": "^3.3.1",
    "json2module": "^0.0.3",
    "rimraf": "2",
    "rollup": "0.52",
    "rollup-plugin-uglify": "2",
    "rollup-pluginutils": "2",
    "rollup-watch": "4",
    "uglify-js": "3"
  },
  "author": "Parashar Sangle - https://github.com/Aqua-4"
}
